## 打包不同环境的apk
1、修改api接口
2、修改应用名称
3、修改包名称

## 打包不同环境的H5
1、修改api接口

## 注意事项
1、此项目根据不同环境兼容处理扫码等功能，但是只在web环境做了与内嵌网页的postMessage交互
2、如果是：打包成app。则iframe只有内嵌显示功能，不能与内嵌的网页交互，有需求需要另外开发
3、如果是：打包成web，再将此项目作为app的内嵌网页运行。则app的运行手机版本不能过低，因为旧版本webview无法处理新的es导入方式